<div class="jumbotron text-center">
  <% if user_signed_in? %>
    <h1> Bievenu <%= current_user.first_name %> 🙋🏿 !</h1>
    <% if current_user.restriction %>
      <h2> Voici les plats adaptés à votre régime !</h2>
    <% else %>
      <h2> Avez-vous un régime particulier ? Nous vous invitons à l'indiquer dans votre profile.</h2>
    <% end %>
    
  <% else %>
    <h1> Bievenu !</h1>
    <h2> Veuillez vous inscrire ou connecter afin d'accéder à toutes les fonctionalités </h2>
  <% end %>
</div>
<div class="container">
  <div class="row">
    <% @meals.each do |meal|%>
      <div class="card col-lg-4" >
        <div class="container text-center">
          <strong>
            <%= link_to meal.name, meals_show_path(meal.id) %>
            <% if user_signed_in? %>
              <% if current_user.favorit == meal %>
                <%= image_tag("favorit.png", :alt => "favorit", :style => "width: 20px;") %>
              <% end %>
            <% end %>
          </strong>
        </div>
        <br>
        <div>
          <% if meal.image_url %>
            <%= image_tag(meal.image_url, :style => "width: 300px;") %>
          <% end %>
        </div>
        <br>
      </div>
    <% end %>
  </div>
</div>
